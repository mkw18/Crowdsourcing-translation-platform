#include "assignqryresult.h"

AssignQryResult::AssignQryResult()
{

}

QList<Assignment> AssignQryResult::sortByLanguage(QList<Assignment> &assignList, const int &Language)
{
    QList<Assignment> goalAssign;
    int pos = 0;
    if (Language ==1)
    {
        for (QList<Assignment>::iterator i = assignList.begin(); i < assignList.end(); i++)
        {

        }
    }
}
